- include: tasks/register.yml 
  vars: 
    nodes: localhost
- include: tasks/register.yml 
  vars: 
    nodes: hosts managers workers
- hosts: managers
  become: true
  roles:
    - common
    - managers
- hosts: managers[0] workers
  become: true
  roles:
    - common
    - workers

